#!/usr/bin/env python3
"""
Coverletter Wizard - Command-line tool for job application content management.

This script is the main entry point for the coverletter_wiz CLI tools.
"""

import os
import sys
from pathlib import Path

# Add the repository root to the Python path
REPO_DIR = Path(__file__).parent.absolute()
sys.path.append(str(REPO_DIR))

# Import and run the CLI
from coverletter_cli import main

if __name__ == "__main__":
    main()
